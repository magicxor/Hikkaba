@model Hikkaba.Web.ViewModels.PostsViewModels.PostEditViewModel
@{
    ViewData["Title"] = $"Edit post {Model.Id}";
}

<form asp-route="PostEditConfirm" asp-route-categoryAlias="@Model.CategoryAlias" asp-route-threadId="@Model.ThreadId" asp-route-postId="@Model.Id" method="post" class="form-horizontal">
    <h4>@ViewData["Title"]</h4>
    <hr />
    <div asp-validation-summary="All" class="text-danger"></div>
    <input asp-for="Id" type="hidden" />

    <div class="form-group">
        <label asp-for="MessageOriginalMarkup" class="col-md-2 control-label"></label>
        <div class="col-md-10">
            <textarea asp-for="MessageOriginalMarkup" class="form-control"></textarea>
            <span asp-validation-for="MessageOriginalMarkup" class="text-danger"></span>
        </div>
    </div>

    <div class="form-group">
        <div class="col-md-offset-2 col-md-10">
            <button type="submit" class="w-100 btn btn-lg btn-primary">Save</button>
        </div>
    </div>
</form>
