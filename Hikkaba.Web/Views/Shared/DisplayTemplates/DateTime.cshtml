@using Hikkaba.Shared.Constants
@model DateTime?

@if (Model.HasValue)
{
    <time class="time-localizable" datetime="@Model.Value.ToString(Defaults.CsharpGlobalDateTimeFormatString)">@Model.Value.ToString(Defaults.CsharpGlobalDateTimeFormatString)</time>
}
else
{
    @("—")
}
